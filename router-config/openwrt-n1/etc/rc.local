# Put your custom commands here that should be executed once
# the system init finished. By default this file does nothing.
# mount -t vfat /dev/sda1 /media/sda1
# service sshd start
# mount -o remount,rw /media/sda1

# umount /mnt/sda1

# sleep 5

# mount -t ext4 /dev/sdb1 /mnt/sda1

# mount -o remount,rw /mnt/sda1

sleep 5

mount -o remount rw /

/kuaicdn/app/ipes/bin/ipes start

sleep 10

/kuaicdn/app/ipes/bin/ipes stop

# mount -t vfat /dev/sda1 /mnt/sda1
# service sshd start
# mount -o remount,rw /mnt/sda1
# /usr/node/ttnode -p /mnt/sda1 #暂时问题没解决
# /kuaicdn/app/ipes/bin/ipes start

/usr/sbin/balethirq.pl
/usr/sbin/balethirq.pl
exit 0
